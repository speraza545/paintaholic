<div class="job-wrapper">
    </br>
    <h1>Please enter your room's details in inches</h1>
    <p>If you would like to add more than one room to your job, please just add the room, and the form will reset</p>
    <p>When you are done adding rooms just hit "view job"</p>

    <form method="post" action = "/jobs/<%= @job.id %>/rooms">
        <input type ="number" name = "length" placeholder="length in inches" required> x 

        <input type ="number" name = "width" placeholder="width in inches" required> x 

        <input type ="number" name = "height" placeholder="height in inches" required>

        <input type="hidden" name = "job_id" value="<%= @job.id %>">
        <input type="submit" name="submit" value="Add Room">
    </form>
    <form method="get" action="/jobs/<%= @job.id %>">
        <input type="submit" name="submit" value="View Job">
    </form>
</div>
<div class = "card-wrapper">
    <h3>Current Rooms:<h3>
</div>
<% if @job.rooms != []%>
<% @job.rooms.each_with_index do |room, index|%>
    <div class="card-wrapper">
        <h3>Room <%= index+1 %>:</h3>
        <p> Length: <%= room.length %>"</br>Width: <%= room.width %>"</br>Height: <%= room.height %>"</p>
    </div>
<% end %>
<% else %>
    <h5>No rooms have been added</h5>
    <h5>Please enter dimensions above</h5>
<% end %>